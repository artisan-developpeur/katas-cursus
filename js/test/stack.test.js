const { assert}  = require('chai')
const Stack = require('../src/stack.js')

describe('Stack', function() {
    it('push should stake the provided value');
    it('pop should provide the latest value');
    it('count should return the stack size');
});